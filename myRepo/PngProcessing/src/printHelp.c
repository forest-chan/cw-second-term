#include <stdio.h>
#include "imageProcessing.h"


void printHelp(){
    printf("\tПо умолчанию последним аргументом программа принимает имя PNG файла, имя файла PNG после обработки - out.png.\n\n"
           "\tДля того чтобы увидеть справку, нужно запустить программу с одним из следующих ключей: --help / -h / -?\n"
           "\t\tПример: ./cw2 -h\n\n"
           "\tДля того чтобы вручную задать имя файла после обратотки необходимо использовать ключ --out / -o\n"
           "\t\tПример: ./cw2 <имя_файла_для_обработки> -o <имя_файла_после_обработки>\n\n"
           "\tКоординаты левого верхнего угла задаются ключом --start / -s, координаты принимаются в виде: x,y\n"
           "\tКоординаты правого нижнего угла задаются ключом --end / -e, координаты принимаются в виде: x,y\n"
           "\t\tПример: ./cw2 --start x1,y1 --end x2,y2 <имя_файла_для_обработки>\n\n"
           "\tДля того чтобы нарисовать квадрат, необходимо использовать ключ --draw-square / -D, который принимает обязательные параметры - длину стороны и толщину линий квадрата в виде: сторона,толщина\n"
           "\tДля того чтобы указать, каким цветом будет залит квадрат, необходимо воспользоваться флагом --fill-color / -f и передать ему аргумент - имя цвета\n"
           "\tДля того чтобы указать цвет линий квадрата, необходимо воспользоваться ключом --line-color / -l и передать обязательный параметр - имя цвета. Если имя цвета будет указано неверно / не указано, то по умолчанию будет выбран черный цвет\n"
           "\t\tПримеры: ./cw2 -D side,depth --start x,y -f <имя_цвета> -l <имя_цвета> <имя_файла_для_обработки>\n"
           "\t\t         ./cw2 -D side,depth --start x,y -l <имя_цвета> <имя_файла_для_обработки>\n\n"
           "\tДля того чтобы поменять местами 4 куска области, необходимо воспользоваться ключом --swap-parts / -S, который принимает обязательный аргумент - режим работы: circle или diagonal\n"
           "\t\tПример: ./cw2 -S diagonal --start x1,y1 --end x2,y2 <имя_файла_для_обработки>\n\n"
           "\tДля того чтобы найти и заменить самый часто встречаемый цвет, необходимо воспользоваться ключом --change-most-frequent-color / -C, который принимает обязательный аргумент - цвет для замены\n"
           "\t\tПример: ./cw2 -C <имя_цвета> <имя_файла_для_обработки>\n\n"
           "\tДля того чтобы инвертировать цвета изображения, необходимо воспользоваться ключом --invert-colors / -I. По умолчанию инвертируются все цвета на картинке\n"
           "\t\tПримеры: ./cw2 -I --start x1,y1 --end x2,y2 <имя_файла_для_обработки>\n"
           "\t\t         ./cw2 -I <имя_файла_для_обработки>\n\n"
           "\tТаблица имен цветов:\n"
           "\tred       >> красный\n"
           "\tblue      >> синий\n"
           "\tgreen     >> зеленый\n"
           "\tblack     >> черный\n"
           "\twhite     >> белый\n"
           "\torange    >> оранжевый\n"
           "\tviolet    >> фиолетовый\n"
           "\tgray      >> серый\n"
           "\tlightblue >> голубой\n"
           "\tyellow    >> желтый\n\n");
}